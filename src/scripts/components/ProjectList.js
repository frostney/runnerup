'use strict';

var React = require('react/addons');

require('styles/ProjectList.less');

var ProjectList = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for ProjectList</p>
        </div>
      );
  }
});

module.exports = ProjectList; 

